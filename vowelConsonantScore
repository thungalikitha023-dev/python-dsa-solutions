class Solution(object):
    def frequencySort(self, s):
        a=""
        f={}
        for i in s:
            f[i]=f.get(i,0)+1
        st=sorted(f.items(),key=lambda x:x[1],reverse=True) 
        for i, j in st:
            a+=i*j
        return a    

             
