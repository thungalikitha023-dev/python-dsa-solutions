class Solution(object):
    def majorityElement(self, nums):
        d={}
        for i in nums:
            d[i]=d.get(i,0)+1
        m=-1
        for i in d:
            if d[i]>m:
                m=d[i]
                ans=i
        return ans        
        
